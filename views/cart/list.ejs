<!DOCTYPE html>
<html lang="ko">
<head>
    <%- include('../partials/head') %>
    <style>
        .cart-container { max-width: 1000px; margin: 40px auto; padding: 20px; }
        
        .cart-table { width: 100%; border-collapse: collapse; margin-bottom: 30px; }
        .cart-table th { background: #f9f9f9; padding: 15px; border-bottom: 2px solid #333; text-align: center; }
        .cart-table td { padding: 15px; border-bottom: 1px solid #ddd; vertical-align: middle; text-align: center; }
        
        .item-info { display: flex; align-items: center; gap: 15px; text-align: left; }
        .item-img { width: 80px; height: 80px; object-fit: cover; border-radius: 4px; border: 1px solid #eee; }
        .item-details h4 { margin: 0 0 5px 0; font-size: 1rem; }
        .item-option { font-size: 0.9rem; color: #666; }

        .summary-box { 
            background: #f8f9fa; padding: 30px; border-radius: 8px; 
            text-align: right; margin-left: auto; width: 300px;
        }
        .summary-row { display: flex; justify-content: space-between; margin-bottom: 10px; font-size: 1rem; }
        .summary-row.total { font-weight: bold; font-size: 1.3rem; border-top: 2px solid #ddd; padding-top: 15px; margin-top: 15px; color: #d63031; }

        .btn-checkout { 
            display: block; width: 100%; padding: 15px; margin-top: 20px;
            background: #333; color: white; border: none; font-size: 1.1rem; cursor: pointer; text-align: center; text-decoration: none;
        }
        .btn-checkout:hover { background: #555; }
        
        .empty-cart { text-align: center; padding: 50px; color: #777; }
        .btn-shopping { display: inline-block; margin-top: 20px; padding: 10px 20px; border: 1px solid #333; color: #333; text-decoration: none; }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="cart-container">
        <h2>장바구니</h2>

        <% if (cartItems.length > 0) { %>
            <table class="cart-table">
                <thead>
                    <tr>
                        <th style="text-align: left; padding-left: 20px;">상품 정보</th>
                        <th>수량</th>
                        <th>상품 금액</th>
                        <th>합계</th>
                        <th>관리</th>
                    </tr>
                </thead>
                <tbody>
                    <% cartItems.forEach(item => { %>
                        <tr>
                            <td>
                                <div class="item-info">
                                    <img src="<%= item.image_url || '/images/no-image.png' %>" class="item-img">
                                    <div class="item-details">
                                        <h4><%= item.product_name %></h4>
                                        <div class="item-option">
                                            옵션: <%= item.option_name %> 
                                            <% if(item.extra_price > 0) { %>(+<%= item.extra_price.toLocaleString() %>)<% } %>
                                        </div>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <%= item.quantity %>개
                            </td>
                            <td>
                                ₩ <%= (item.base_price + item.extra_price).toLocaleString() %>
                            </td>
                            <td style="font-weight: bold;">
                                ₩ <%= item.total_price.toLocaleString() %>
                            </td>
                            <td>
                                <button onclick="deleteItem(<%= item.item_id %>)" style="background:none; border:none; color:red; cursor:pointer;">삭제</button>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>

            <div class="summary-box">
                <div class="summary-row">
                    <span>총 상품 금액</span>
                    <span>₩ <%= totalProductPrice.toLocaleString() %></span>
                </div>
                <div class="summary-row">
                    <span>배송비</span>
                    <span>
                        <% if (deliveryFee === 0) { %>
                            무료
                        <% } else { %>
                            + ₩ <%= deliveryFee.toLocaleString() %>
                        <% } %>
                    </span>
                </div>
                <div class="summary-row total">
                    <span>결제 예정 금액</span>
                    <span>₩ <%= finalPrice.toLocaleString() %></span>
                </div>

                <a href="/checkout" class="btn-checkout">주문하기</a>
            </div>

        <% } else { %>
            <div class="empty-cart">
                <h3>장바구니에 담긴 상품이 없습니다.</h3>
                <a href="/products" class="btn-shopping">쇼핑하러 가기</a>
            </div>
        <% } %>
    </main>

    <%- include('../partials/footer') %>

    <script>
        // 삭제 기능은 다음 이슈에서 구현합니다. (임시 스크립트)
        function deleteItem(itemId) {
            if(confirm('정말 삭제하시겠습니까? (기능 구현 중)')) {
                alert('다음 이슈에서 AJAX로 구현됩니다! ID: ' + itemId);
            }
        }
    </script>
</body>
</html>